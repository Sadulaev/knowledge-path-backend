export type UserSignUpType = {
    name: string;
    email: string;
    password: string;
}

export type UserSessionDataType = {
    device: string;
    ip: string;
}

export type UserAuthType = {
    email: string;
    password: string;
}